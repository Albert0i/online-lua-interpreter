// routes/api.js
import express from 'express';
const router = express.Router();

// Placeholder for future endpoints
router.post('/eval', (req, res) => {
  res.json({ message: 'Eval endpoint not yet implemented' });
});

export default router;
